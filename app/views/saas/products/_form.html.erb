<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :stripe_id, "Stripe ID" %>
    <%= form.text_field :stripe_id %>
  </div>

  <div class="field">
    <%= form.label :statement_descriptor %>
    <%= form.text_field :statement_descriptor %>
  </div>

  <div class="field">
    <%= form.label :unit_label %>
    <%= form.text_field :unit_label %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
